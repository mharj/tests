/*
 TS Generics
   1. Implement a "Nullable" type that allows a given type T to also be null or undefined.
   2. Modify demo1 so that it accepts a Nullable string as an argument.
   3. Modify demo2 to:
     - Use generics to accept any Nullable type.
     - Ensure the return type is the non-nullable version of the provided generic type. 
*/

export function demo1(arg: string): string {
	throw new Error('missing implementation');
}

export function demo2(arg: any) {
	throw new Error('missing implementation');
}
